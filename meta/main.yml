# meta/main.json
---
dependencies:
  - role: geerlingguy.ntp
    vars: 
      # I don't want chrony (oldmanyellsatcloud.jpg)
      ntp_daemon: ntpd
      ntp_package: ntp
      ntp_config_file: /etc/ntp.conf
      ntp_manage_config: true
      ntp_area: us
